# 🎓 Telegram-бот для проверки заданий

Удобный инструмент для организации учебного процесса в Telegram. Бот позволяет преподавателям выдавать задания и проверять их, а студентам — сдавать работы и получать оценки в режиме реального времени.

## 🌟 Основные возможности

### 👨‍🎓 Для студентов
- **Присоединение к группам**: Вступление в учебную группу по уникальной ссылке-приглашению.
- **Управление заданиями**: Просмотр списка актуальных заданий с описанием, прикрепленными файлами и сроками сдачи.
- **Сдача работ**: Отправка ответов прямо в чат бота.
- **Контроль успеваемости**: Просмотр оценок (0-100 баллов) и комментариев преподавателя к каждой работе.
- **Уведомления**: Мгновенные оповещения о новых заданиях и результатах проверки.

### 👨‍🏫 Для преподавателей
- **Управление группами**: Создание учебных групп и генерация ссылок для приглашения студентов.
- **Создание заданий**: Гибкая настройка заданий с поддержкой текста, файлов (фото/документы) и установкой дедлайнов.
- **Проверка работ**: Удобный интерфейс для просмотра ответов, выставления баллов и написания фидбека.
- **Массовая рассылка**: Назначение одного задания сразу нескольким группам.

### 🔧 Для администраторов
- **Управление доступом**: Возможность блокировки и разблокировки пользователей по ID.
- **Полный контроль**: Доступ ко всем инструментам системы.

---

## 🛠 Технологический стек
- **Язык**: Python 3.13+
- **Фреймворк**: aiogram 3.x (асинхронный)
- **База данных**: MySQL 8.0 (SQLAlchemy 2.0 + aiomysql)
- **Кэширование и FSM**: Redis (redis-py)
- **Валидация данных**: Pydantic v2
- **Логирование**: Concurrent log handler (с разделением по модулям: bot, db, redis)
- **Контейнеризация**: Docker & Docker Compose

---

## 🚀 Быстрый запуск

### Предварительные требования
1. Установленный **Docker** и **Docker Compose**.
2. Полученный токен бота у [@BotFather](https://t.me/BotFather).

### Установка и запуск (Docker)
1. Склонируйте репозиторий:
   ```bash
   git clone <url_репозитория>
   cd telegram_bot_for_checking_assignments
   ```

2. Создайте файл `.env` на основе примера:
   ```bash
   cp .env.example .env
   ```

3. Заполните в `.env` ваш `BOT_TOKEN` и другие необходимые параметры.

4. Запустите проект:
   ```bash
   docker-compose up -d --build
   ```

После этого бот будет доступен в Telegram, а база данных автоматически инициализируется начальной структурой.

---

## ⚙️ Конфигурация (.env)
Основные параметры настройки:
- `BOT_TOKEN` — токен вашего бота.
- `ADMIN_ID` — ID главного администратора (можно узнать через @userinfobot).
- `MYSQL_*` — настройки подключения к базе данных.
- `REDIS_*` — настройки подключения к Redis.
- `LOG_LEVEL` — уровень логирования (DEBUG, INFO, ERROR).

---

## 📁 Структура проекта
```text
├── src/
│   ├── bot/            # Логика Telegram-бота (хендлеры, фильтры, клавиатуры)
│   ├── core/           # Ядро (настройки, логирование, контекст приложения)
│   ├── db/             # Слой работы с БД (модели, репозитории, миграции)
│   ├── redis/          # Клиенты для работы с Redis (кэш ролей, админов)
│   ├── services/       # Бизнес-логика и хранилища
│   └── utils/          # Вспомогательные утилиты
├── logs/               # Директория с логами
├── run.py              # Точка входа для запуска
└── docker-compose.yml  # Описание инфраструктуры
```

---

## 🤝 Поддержка и разработка
Если у вас возникли вопросы или предложения по улучшению, пожалуйста, создайте Issue или свяжитесь с администратором.
